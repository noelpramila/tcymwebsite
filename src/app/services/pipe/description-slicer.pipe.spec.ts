import { DescriptionSlicerPipe } from './description-slicer.pipe';

describe('DescriptionSlicerPipe', () => {
  it('create an instance', () => {
    const pipe = new DescriptionSlicerPipe();
    expect(pipe).toBeTruthy();
  });
});
